{
  "name": "langgraph-minimal",
  "version": "1.0.0",
  "description": "Minimal LangGraph Monorepo with TypeScript",
  "private": true,
  "workspaces": [
    "packages/*",
    "gateways/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test",
    "clean": "pnpm --recursive clean"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "@langchain/openai": "^0.1.3",
    "@openai/agents": "^0.0.17",
    "dotenv": "^17.2.1",
    "openai": "^5.16.0"
  }
}
